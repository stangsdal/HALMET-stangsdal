diff --git a/src/HALMET_ClipperDuet.cpp b/src/HALMET_ClipperDuet.cpp
index 512efcd..204a0c6 100644
--- a/src/HALMET_ClipperDuet.cpp
+++ b/src/HALMET_ClipperDuet.cpp
@@ -3,6 +3,10 @@
 #include <N2kMessages.h>
 #include <string.h>
 
+#if defined(HAMLET_CLIPPERDUET_HAS_SPI_CAPTURE)
+#include <ESP32SPISlave.h>
+#endif
+
 namespace hamlet {
 namespace clipperduet {
 
@@ -387,33 +391,60 @@ bool Decoder::BuildDistanceLogMessage(tN2kMsg& msg, double days_since_1970,
 #if defined(HAMLET_CLIPPERDUET_HAS_SPI_CAPTURE)
 Esp32SpiCapture::Esp32SpiCapture(const Pins& pins) : pins_(pins) {}
 
+Esp32SpiCapture::~Esp32SpiCapture() {
+  auto* slave = static_cast<ESP32SPISlave*>(slave_);
+  if (slave != nullptr) {
+    slave->end();
+    delete slave;
+    slave_ = nullptr;
+  }
+}
+
 void Esp32SpiCapture::Begin() {
-  slave_.setDataMode(SPI_MODE3);
-  slave_.begin(HSPI, pins_.clk, pins_.miso, pins_.mosi, pins_.cs);
+  auto* slave = static_cast<ESP32SPISlave*>(slave_);
+  if (slave_ == nullptr) {
+    slave = new ESP32SPISlave();
+    slave_ = slave;
+  }
+
+  slave->setDataMode(SPI_MODE3);
+  slave->begin(HSPI, pins_.clk, pins_.miso, pins_.mosi, pins_.cs);
   memset(rx_buf_, 0, sizeof(rx_buf_));
 }
 
 bool Esp32SpiCapture::ReadFrame(uint8_t* out_frame, size_t* out_size) {
-  if (!out_frame || !out_size) {
+  auto* slave = static_cast<ESP32SPISlave*>(slave_);
+  if (!out_frame || !out_size || slave == nullptr) {
     return false;
   }
 
-  if (slave_.remained() == 0) {
-    slave_.queue(rx_buf_, kFrameBufferSize);
+  if (slave->numTransactionsCompleted() == 0) {
+    if (slave->numTransactionsInFlight() == 0) {
+      if (slave->queue(nullptr, rx_buf_, kFrameBufferSize)) {
+        slave->trigger();
+      }
+    }
+    return false;
   }
 
-  if (!slave_.available()) {
+  size_t frame_size = slave->numBytesReceived();
+  if (frame_size == 0) {
     return false;
   }
-
-  size_t frame_size = slave_.size();
   if (frame_size > kFrameBufferSize) {
     frame_size = kFrameBufferSize;
   }
 
   memcpy(out_frame, rx_buf_, frame_size);
   *out_size = frame_size;
-  slave_.pop();
+
+  if (slave->numTransactionsInFlight() == 0 &&
+      slave->numTransactionsCompleted() == 0) {
+    if (slave->queue(nullptr, rx_buf_, kFrameBufferSize)) {
+      slave->trigger();
+    }
+  }
+
   return true;
 }
 #endif
diff --git a/src/HALMET_ClipperDuet.h b/src/HALMET_ClipperDuet.h
index 4138c59..f138f0d 100644
--- a/src/HALMET_ClipperDuet.h
+++ b/src/HALMET_ClipperDuet.h
@@ -5,7 +5,6 @@
 
 #if defined(ARDUINO_ARCH_ESP32) && __has_include(<ESP32SPISlave.h>)
 #define HAMLET_CLIPPERDUET_HAS_SPI_CAPTURE 1
-#include <ESP32SPISlave.h>
 #endif
 
 class tN2kMsg;
@@ -118,6 +117,10 @@ class Esp32SpiCapture {
   };
 
   explicit Esp32SpiCapture(const Pins& pins);
+  ~Esp32SpiCapture();
+
+  Esp32SpiCapture(const Esp32SpiCapture&) = delete;
+  Esp32SpiCapture& operator=(const Esp32SpiCapture&) = delete;
 
   void Begin();
 
@@ -126,7 +129,7 @@ class Esp32SpiCapture {
 
  private:
   Pins pins_;
-  ESP32SPISlave slave_;
+  void* slave_ = nullptr;
   uint8_t rx_buf_[kFrameBufferSize]{};
 };
 #endif
